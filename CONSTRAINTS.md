# 前端滚动位置稳定性约束

处理日志、无限列表、历史记录预加载、路由离开后返回等“保持用户当前位置”的问题时，优先使用下面的方案。

## 最优解：锚点 + 相对偏移

不要只保存 `scrollTop`，也不要用裸 `offsetTop` 直接还原位置。它们在列表前插入内容、容器布局变化、KeepAlive 失活、字体/行高重新计算时都容易产生偏移。

应当记录：

- 当前视口内第一条可见记录的稳定标识，例如日志的 `timestamp:id`
- 该记录相对滚动容器视口顶部的偏移，使用 `getBoundingClientRect()` 计算

恢复时：

- 等 DOM 更新完成后再恢复，通常是 `nextTick()` 后接 `requestAnimationFrame()`
- 找到同一条记录
- 用“当前相对偏移 - 原相对偏移”调整 `scrollTop`

核心公式：

```ts
const beforeOffset = row.getBoundingClientRect().top - container.getBoundingClientRect().top
container.scrollTop += beforeOffset - savedOffset
```

## 必须避免

- 不要只保存 `scrollTop`
- 不要在 DOM 已经离开页面后才首次读取位置
- 不要让失活后的不稳定读数覆盖之前的稳定位置
- 不要依赖数组下标作为长期锚点；下标只适合“本次前插后立即恢复”的短窗口场景

## 实现要求

- 在用户仍停留在页面且处于暂停/浏览状态时持续刷新“最后稳定位置”
- `element.isConnected === false` 时不要记录或恢复
- 列表记录必须提供稳定的 DOM 标识，例如 `data-log-key`
- 找不到锚点时不强行恢复，保持当前滚动位置
- 新增或修改此类逻辑时，必须加测试覆盖：
  - 前插内容后仍保持同一记录的视口偏移
  - 滚动容器自身坐标变化后不产生额外偏移
  - DOM 已断开时不会记录不稳定位置

这个方案已在日志暂停返回和无限过往日志预加载场景中验证有效。

# 日志列表虚拟滚动约束

日志列表只渲染视口窗口内的行。窗口外的行不在 DOM 里，任何依赖「所有行都能查到」的逻辑都要改成按布局换算。

滚动协调集中在 `client/log-viewport.ts`：本节与上节的硬约束在那里都有对应的注释锚点，改动前先读它。它通过 `client/viewport-host.ts` 的四方法 seam 与 DOM 隔开，`client/use-log-viewport.ts` 只负责 ref 绑定、生命周期与调度注入，布局算法留在 `client/virtual-list.ts`。

## 占位必须用内边距，不能用 transform

窗口外的高度折进占位容器的 `padding-top` / `padding-bottom`。不要用 `transform: translateY()` 位移窗口：`offsetTop` 是布局值，不含 transform，锚点恢复与滚动偏移换算会整段偏掉。同理，滚动容器要自己充当 `offsetParent`（`position: relative`），否则日志行的 `offsetTop` 不等于它在滚动坐标里的位置。

## 行高按稳定标识缓存，留白必须计入行高

行高不固定：正文会换行，跨重启的分隔行还多一段留白。实测高度按 `timestamp:id` 缓存，前插历史日志后仍然有效；未量过的行用估算行高顶位，估算值取排版令牌的实测值，不在脚本里另写字面字号。

行与行之间的间距一律用 `padding`，不用 `margin`：外边距既不进 `offsetHeight` 也不进 `getBoundingClientRect().height`，占位高度会比真实内容矮一截，越往下偏得越多。`:first-child` 这类位置选择器同样不能用——窗口里的第一行不是列表的第一行。

## 改占位高度前先取锚点

实测高度一旦写回，占位高度随之变化，视口里的内容就已经移动了。锚点必须在改动之前取，改完再按锚点把 `scrollTop` 修回去，否则读到的是移动后的位置，修正等于没做。同一帧内只允许一处改 `scrollTop`：手动恢复位置期间要挡住逐帧的高度修正，两处同时改会互相打断。

闸门必须覆盖完整异步变更，并在每个 DOM 刷新边界核对旧帧是否已失效。测量改变占位后先提交渲染，再按实际矩形调整位置；不能用尚未刷新的矩形修正。日志容器需设置 `overflow-anchor: none`，避免 Firefox 的原生滚动锚定在宽度重排后重复补偿。

## 闸门只覆盖「内容已改、位置还没修」的那一段

闸门管的是「谁能改滚动位置」，不是「谁能刷新渲染窗口」。读一页历史要跨很多帧，若从发起请求就关着闸门等，渲染窗口会冻在请求开始时的位置：用户继续滚只会滚进一片空白，请求回来还会被拉回点击时的锚点。请求在途期间内容尚未改变，本来就不需要独占滚动位置，逐帧的锚点补偿照常跟着用户走；闸门等到内容真正落地再关。

同理，一帧在等待 DOM 刷新期间被别处接管闸门时，必须重新挂起待办等释放后补做。帧开头已经把待办清掉了，直接返回会把「换窗 + 测量 + 贴底」整段丢掉，而下一条日志不一定会来。

浏览器可先修改 `scrollTop`、后派发 `scroll`。不能只依赖事件推进的代次裁决自动贴底：清单提交、帧启动及自动贴底每个异步边界都要先核对实际位置，发现未处理的用户移动时先更新意图，使旧写入失效。内容收缩造成的 `min(旧位置, 新最大位置)` 钳制不属于独立用户移动，不得据此改变追踪状态。显式点击回底应以点击当时的位置作为基线，只让点击之后的新滚动取消它。回归测试必须覆盖未派发事件的回顶、持续刷新、自绘滑块拖顶与原生钳制；不能总在改位置后立即调用 `handleScroll()`，否则测不到这类竞态。

## 清单变化本身就会移动视口，锚点要在提交 DOM 之前取

渲染层的 `setItems` 由 Vue 的 pre-flush watcher 调用，此刻 DOM 还是变更前的样子，读到的才是用户真正看到的位置。等到下一帧再取锚点就晚了：

- 实时缓冲是定长的，写满之后每来一条新日志就从头部淘汰最早一条。窗口下标没变、里面的记录却整体换人，视口内容已经先上移了一次，这时取锚点只会把这次位移固化下来。
- 滚动与新日志几乎总是交错发生。用户滚动时换窗被刻意押后到下一帧的锚点保护里，`setItems` 若按新位置提前发布窗口，新进窗口、尚未实测的长日志一渲染就把视口顶下去一大截。因此暂停浏览期间 `setItems` 不得发布新窗口。

取锚点与真正复位之间隔着至少一帧，这期间用户可能又滚了一段。锚点要连同当时的 `scrollTop` 一起记，复位时按「现在滚到哪 − 当时滚到哪」把目标偏移平移过去，既撤掉清单变化造成的位移，又保留用户自己滚出来的位移。

## 暂停浏览期间不得裁剪实时缓冲

定长实时缓冲淘汰的是清单前端。用户贴底追踪时被淘汰的都是早已滚出视口的内容；用户上滚读历史时被淘汰的正是他正在读的那一段——可滚动范围持续收缩，到达顶部后连位置都保不住，表现出来就是「滚动被拉回原位」「刚滚过去的日志消失」。暂停期间必须挂起裁剪，恢复追踪后再补做，并用一个放宽后的硬上限兜底，避免长时间停在历史位置时缓冲无限增长。

## 自绘滑块使用稳定的记录坐标

变高虚拟列表的 `scrollTop / (scrollHeight - clientHeight)` 不是稳定的阅读进度。逐步测量历史长日志时，上方占位增高，锚点补偿会同时增加 `scrollTop`；正文位置保持正确，像素比例却可能增大，使向上滚动的滑块反向回退。

日志自绘滑块先用「记录下标 + 行内比例」定位视口上下边界，再按每条日志的文本预估高度换算成稳定的加权距离。长 JSON 应比一行短消息占据更多距离，不再按一条一票计算进度。只发布换窗、测量和补偿完成后的几何；实测只用于定位行内比例，不得修改稳定的文本预估权重。布局仍用原生像素坐标与 padding 占位，不能为稳住滑块删掉正文补偿，也不能用单向钳制掩盖进度错误。

位置与尺寸必须分开计算：真实可见记录范围只用于定位，不得再用「一屏可见记录数 / 总记录数」控制长度，否则短日志区滑块变长、长日志区变短。日志滑块尺寸按整份清单的文本预估总高度与视口高度计算，不受逐行实测或阅读位置影响；未提供文本预估的调用方才使用统一行高。这是稳定的尺寸估计，不是实际像素总高度比例。尺寸最多占半条轨道，保证仅一条超长日志也能拖动；非日志容器保留原生像素比例。尺寸回归测试的数据量必须能让滑块超过最小长度，避免被 28px 下限掩盖问题，同时覆盖拖拽松手后的长度。

拖拽必须通过同一记录坐标反算落点，先渲染测量目标窗口再求解；不得把记录比例直接乘 DOM 总高度。拖拽期间固定按下时的轨道与滑块尺寸，避免实测高度变化把滑块从指针下挪走。非日志容器继续使用原生像素坐标。

回归测试需同时断言正文锚点、实际滑块 top 的单调性、到顶/到底、未测量区域的拖拽正反映射，以及异步测量期间不发布临时几何。只检查正文位置无法发现滑块回退。

## 文本高度预估不能进入逐帧滚动路径

`log-height-estimator.ts` 按日志身份缓存原始文本行宽与当前列宽下的换行数。扫描需处理日期/等级前缀、来源名、多行缩进、ANSI、宽字符及跨重启分隔留白；不创建隐藏日志 DOM，不逐字调用文字测量 API。等宽字符采用字号比例近似，复杂 Emoji、字体回退及浏览器换行仍可能产生误差，不承诺完全匀速。

- 新日志只扫描新增或正文发生变化的记录；调整宽度复用文本行宽缓存，重新计算换行数。普通滚动只查询布局前缀和缓存，不扫描正文。
- 按字符预算分批让出事件循环，单条巨大 JSON 也必须能够让出，不能只按记录条数分批。过期请求不发布旧清单，销毁后不再提交，缓存随清单卸载清理。
- `prepared-log-list.ts` 在批次完成后一次提交记录与预估。处理期间保持上一份可滚动清单，不允许先替换 DOM、几帧之后再补估算。历史加载的视口事务应等待预估提交，而不是只等网络响应。
- 真实高度与稳定预估高度分别维护累计偏移。实测修正只能改变真实高度表；新增、裁剪、前插及排版条件变化才更新对应预估。

## 容器宽度变化要丢弃实测高度

宽度一变，换行结果全部失效，缓存的行高不能再用。用 `ResizeObserver` 只在宽度真的变化时清空缓存并重量，同时按锚点保住当前位置；高度变化不触发清空。

## 实现要求

- 新增或修改虚拟滚动逻辑时，必须加测试覆盖：
  - 只取窗口内的条目，窗口外高度进入上下占位
  - 前插条目后已实测高度仍然沿用，偏移整体下移
  - 条目被移出清单后实测缓存不会无限增长
  - 十万条条目仍能二分定位窗口，不展开数组
  - 新日志与用户滚动交错到来时不产生额外位移
  - 清单头部被淘汰后暂停浏览的位置仍不动
  - 等历史日志期间渲染窗口继续跟随滚动，加载完成不拉回旧位置
- 假滚动容器必须照真实提交顺序来：`setItems` 先进视口核心，新清单随下一次渲染才进「DOM」。反过来写会让「变更前取锚点」永远测不出问题。
- 只渲染窗口内的行意味着浏览器原生查找（Ctrl+F）与跨屏拖选只覆盖已渲染的行，需要全量检索时使用插件自带的关键词筛选。

# 日志文件规模约束

日志文件数会长到十万量级：单个文件上限默认 10 KB，一次刷屏就能在一天里造出十万个文件。凡是按文件数或文件清单规模伸缩的代码，都必须按「单日十万个文件」来写。

## 不要展开大数组

不得用 `Math.max(...indexes)`、`Math.min(...)`、`push(...items)` 这类展开写法处理文件清单。实参个数有引擎上限，单日文件数上万就是 `RangeError: Maximum call stack size exceeded`。

这一条不是理论风险，已经发生过：分配下一个文件序号时展开整份序号清单，崩溃点正好落在「写一条日志」的路径上，于是进程崩溃、重启、扫到同一批文件、再崩，成了每几秒一轮的死循环，控制台完全打不开。改法是单独维护当前最大序号，分配时只做一次两参数比较（见 `src/log-file-index.ts`）。

## 清理失败不得写日志

过期清理与日志读取都会遇到「文件刚被删掉」，此时不得调用 `ctx.logger(...).warn/error`。写一条日志就要往日志文件里写一行，写满就滚动，滚动又触发清理，清理再报错——自己喂自己，这正是把文件数推到十万的那个循环。`ENOENT` 一律忽略，其余错误才上报。

## 后台清理必须先摘清单再删文件

清理是不可等待的后台任务，会与其他清理、与文件滚动交叠。必须先把过期日期从内存清单里摘掉、再去删磁盘文件；顺序反过来，第二次清理会看到同一批文件并重删一遍，第二遍全是「文件不存在」。摘清单这一步要做成「取走所有权」的语义，让并发的第二次调用拿到空清单，而不是靠加锁或加标记。

## 实现要求

- 新增或修改序号分配、过期清理、日志读取时，必须加测试覆盖：
  - 单日十万级文件仍能分配序号
  - 清理进行中不断滚动文件不产生任何日志
  - 读取到已被删除的文件不产生任何日志
- 清理只挂在启动与跨日两处，不要挂在按大小滚动那一步：新的过期只随日期推进发生，跟文件写满无关。
