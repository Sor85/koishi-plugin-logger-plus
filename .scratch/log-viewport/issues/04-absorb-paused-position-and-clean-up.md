# 04 — 并入暂停位置 module 并清理临时守卫

**What to build:** 收尾。暂停位置 module 的两个函数并入核心实现、文件删除；02 留下的那 4 个临时正则用例删除。日志视口的知识从此集中在一个 module 里，读一处就能理解整条链路。

暂停位置 module 没有通过删除测试：它那二十行会搬进一个已经在做两阶段恢复的函数，复杂度是搬家而非集中。它的 4 条测试锁的前提是「所有行都在 DOM 里」，而虚拟滚动上线后这个前提已经不成立——覆盖已被 03 的前插与窗口外恢复两条取代，且新的前提更贴近真实。

**为什么单独成张**：spec 定下的护栏是「新测试绿了才准删旧的」。把删除动作单独放在一张被 03 阻塞的工单里，这条护栏才是可执行的，而不只是一句叮嘱。

**Blocked by:** 03 — 补 8 条 LogViewport 行为测试

**Status:** ready-for-agent

- [ ] 暂停位置 module 的两个函数并入核心，其文件删除
- [ ] 该 module 的 4 条测试删除（覆盖已由 03 的前插与窗口外恢复两条取代）
- [ ] 02 留下的 4 个临时正则用例删除
- [ ] **两条 CSS 守卫保留**：占位不得使用位移变换、滚动容器必须自己充当定位父级。它们防的是「有人写了不该写的 CSS」，没有运行时行为可测，正则是恰当表达，且直接对应约束文档里的硬约束
- [ ] 虚拟列表布局 module 的 11 条测试仍在且未改
- [ ] `yarn test` 全绿
- [ ] `yarn build` 通过
- [ ] 浏览器中人工复核一遍全部滚动行为
