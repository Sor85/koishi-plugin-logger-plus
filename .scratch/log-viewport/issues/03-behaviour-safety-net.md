# 03 — 补 8 条 LogViewport 行为测试

**What to build:** 视口协调第一次拿到真正的测试面。用一个字面量假 host、由测试手动推进帧，在**无 DOM 环境**下驱动整套滚动行为，断言最终停在哪一行——而不是断言源码里有没有某几行字。

这是四张里第一张能自动验证的：前两张只能靠既有测试加肉眼。做完之后，「日志页滚动改坏了不会报错、只会感觉不对」这个状态才算终结。

不引入 jsdom、happy-dom 或组件测试框架，测试命令保持现在这套裸 `node --test`。

**Blocked by:** 02 — 把视口编排搬进 LogViewport 核心

**Status:** ready-for-agent

- [ ] 假 host 是字面量对象，实现 `ViewportHost` 四方法；帧由测试手动推进，不等待真实帧调度
- [ ] **追踪时追加日志** → 滚动位置等于「总高度减视口高度」
- [ ] **向上滚** → 转入暂停；此后追加日志 → 滚动位置不变
- [ ] **`around` 中前插 20 条** → 视口第一条的相对偏移与前插前相等
- [ ] **实测高度覆盖估算高度** → 视口第一条的相对偏移不变
- [ ] **锚点滑出渲染窗口后 `restore`** → 该行重新进入窗口且相对偏移等于原值
- [ ] **容器宽度变化** → 实测高度丢弃、总高度回落估算，且相对偏移不变
- [ ] **`followLatest()`** → 转入追踪且滚到底
- [ ] **`around` 内部制造一次会触发高度修正的变化** → 全程 `host.scrollTo` **只被调用一次**（「同一时刻只有一处改滚动位置」第一次拿到可执行断言）
- [ ] 断言的全部是外部可观察行为，无一条断言实现细节或源码文本
- [ ] 未新增任何 devDependency
- [ ] `yarn test` 全绿
